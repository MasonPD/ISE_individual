<%= form_for(current_user.circles.find_by_movie_id(@movie),
             html: { method: :delete },
             remote: true) do |f| %>
    <%= f.submit "UnJoin", class: "btn btn-large" %>
<% end %>